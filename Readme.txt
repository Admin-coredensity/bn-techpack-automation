BN Techpack landing page site


1.) custom code (logic) -> (html/javascript/css/markup(.md files, product html)) -> git
2.) custom data (media) (video(youtube)/images(pininterest)/icons/fonts/) -> cdn (cloudfront/s3/versel/cdnjs/etc..)
3.) json data (database) -> (firebase store/air table) or fetch api or sdk to run query 


MVC -> model(data)/view(html)/controller(logic)
